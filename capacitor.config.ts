import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lindadata.fieldcopilotpro",
  appName: "Field Copilot Pro",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
